/*var dir = point_direction(x,y,other.x,other.y);

xSpeed = spd * cos(degtorad(dir));
ySpeed = spd * sin(degtorad(dir));

if(x > other.x){
	absolutePosX += spd;
}
else absolutePosX -= spd;

if(y > other.y){
	absolutePosY += spd;
}
else absolutePosY -= spd;*/